# AngularJS + Rails3 port of Javascript Projects Web App (demo)

## Local installation

    bundle install --without production
    rake db:migrate
    rake db:seed
    rails server

open [http://localhost:3000](http://localhost:3000/)
